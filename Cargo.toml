[package]
name = "capybara"
version = "0.2.2+perf.2"
edition = "2021"
license = "GPL-3.0-only"
description = "Emulateur bare-metal du Sonix SNC73410, compatible avec le firmware Tamagotchi Paradise"
repository = "https://github.com/knuckleslee/capybara"

[dependencies]
# Moteur wgpu et non glow. Sur Windows, le compositeur du bureau ignore la
# couche alpha des fenetres OpenGL : la fenetre du mode jeu restait sur fond
# noir au lieu d'etre decoupee. wgpu passe par Direct3D et compose correctement.
eframe = { version = "0.29", default-features = false, features = [
    "accesskit",
    "default_fonts",
    "wgpu",
    "wayland",
    "x11",
] }
egui = "0.29"
rodio = "0.19"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }
rand = "0.8"
directories = "5.0"
byteorder = "1.5"
rfd = "0.15"
log = "0.4"
# Decodage des images de fond glissees sous la fenetre transparente. Seuls les
# formats qu'on rencontre vraiment sont actives, le reste alourdirait pour rien.
image = { version = "0.25", default-features = false, features = [
    "png",
    "jpeg",
    "bmp",
    "gif",
    "webp",
] }
tray-icon = "0.19.2"
# Le handle de la fenetre, pour poser la couleur de transparence de Windows
# quand la carte graphique refuse la composition par pixel.
raw-window-handle = "0.6"
# Verification des mises a jour, a la demande. Un client bloquant suffit :
# l'appel a lieu dans un fil dedie et une seule fois par clic.
ureq = { version = "2.10", default-features = false, features = ["tls", "gzip"] }

[target.'cfg(windows)'.build-dependencies]
winresource = "0.1"

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
serialport = { version = "4.8", default-features = false }

# Le coeur est un interpreteur : il passe son temps dans une poignee de
# fonctions qui s'appellent des millions de fois par seconde. L'optimisation
# entre modules et une seule unite de generation permettent de les fondre, ce
# qui compte bien plus ici que le temps de compilation.
[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"

# Paquet Debian, construit par cargo-deb sur le runner Ubuntu. Les dependances
# sont celles que rodio, rfd et winit reclament a l'execution : le son, la
# boite de dialogue de fichiers, et le serveur graphique.
[package.metadata.deb]
maintainer = "infinition"
copyright = "2026, infinition"
license-file = ["LICENSE", "0"]
extended-description = """
Emulateur bare-metal du Sonix SNC73410, compatible avec le firmware Tamagotchi
Paradise. Ne contient aucune donnee de console : il vous faut le dump de votre
propre appareil et sa cle."""
section = "games"
priority = "optional"
depends = "$auto"
assets = [
    ["target/release/capybara", "usr/bin/", "755"],
    ["assets/capybara.desktop", "usr/share/applications/", "644"],
    ["assets/icone.png", "usr/share/icons/hicolor/256x256/apps/capybara.png", "644"],
    ["README.md", "usr/share/doc/capybara/README.md", "644"],
]
